/* Reset */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}
body {
  font-family: -apple-system, BlinkMacSystemFont, sans-serif;
  background: #ffffff;
  color: #000000;
}

/* Top navigation */
.top-nav {
  display: flex;
  justify-content: center;
  gap: 8rem;
  padding: 2.1rem 0;
  background: #111111;
  border-bottom: 1px solid #727272;
}
.top-nav a {
  color: #bababa;
  text-decoration: none;
}

/* Main content wrapper */
.main-content {
  transition: margin-left 0.3s ease;
  margin-left: 0;
  position: relative; /* anchor for toggle button */
  padding: 1 rem 0;
}

/* When sidebar is open */
.main-content.shift {
  margin-left: 280px; /* same width as sidebar */
}

/* Viewer */
.viewer-container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
.viewer-box {
  width: 90%;
  height: 70%;
  max-width: 900px;
  height: 900px;
  background: #ffffff;
  overflow: hidden;
}
.viewer-box iframe {
  width: 100%;
  height: 70%;
    border: none;
}
/* Controls */
.controls {
  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
  gap: 2rem;
  margin: 1.5 rem 0;
}
.controls p {
  font-weight: bold;
}
.controls label {
  display: flex;
  align-items: center;
}
.controls input {
  margin-right: 0.5rem;
}

/* Slides */
.slides {
  width: 75%;
  margin: 0 auto 2rem;
  text-align: center;

}
.slides h1 {
  margin-top: 0;
  margin-bottom: 1rem;
  font-weight: 400;
}
.slides p {
  color: #535353;
  line-height: 1.6;
  margin-bottom: 1rem;
  font-weight: 300;

}
/* ─── TEMP HIDE SIDEBAR ─── */
.sidebar {
  display: none !important;
}


/* ─── RESTORE VERTICAL GUTTERS ─── */
/* between nav and viewer */
.viewer-container {
  padding-top: 0rem;  
  padding-bottom: 0rem;
}

/* between checkboxes and SSM text */
.controls {
  margin-bottom: 0rem; /* puts space under the row of checkboxes */
}

/* add breathing room around the slide block */
.slides {
  margin: 2rem auto 3rem; /* 2rem above, 3rem below */
}
/* ─── SSM SECTION ─── */
.ssm-section {
  background-color: #eeeeee;       
  border-top: 1px solid #8e8e8e;   
  padding: 4.5rem 0rem;           
  margin-top: 2rem;    
  width: 100%;
  height: 100%;
}

.ssm-section .slides {
  text-align: center;
}
.ssm-section .slides h1 {
  margin-bottom: 2rem;
}
.ssm-section .slides p {
  margin-bottom: 1rem;
  line-height: 2;
}

.top-nav {
  /* your existing rules… */

  /* add a small dark bar above the nav */
  border-top: 30px solid #1d1d1d; 
 
}

.theme-toggle {
  position: fixed;
  top: 1rem;
  right: 1rem;
  background: transparent;
  border: none;
  font-size: 1.2rem;
  cursor: pointer;
  color: var(--text-color); /* if you’re using variables */
}
